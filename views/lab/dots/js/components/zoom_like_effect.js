Class(SerpentityApp.Components, "ZoomLikeEffect").inherits(Serpentity.Component)({
    prototype : {
        zoomSize : 50,
        effectArea : 200,
        targetEntity : null,

        _originalPosition : null
    }
});
